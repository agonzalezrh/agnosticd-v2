---
- name: Step 005 Post Software
  hosts: localhost
  connection: local
  become: false
  gather_facts: false
  tasks:
  - name: Print stage message
    ansible.builtin.debug:
      msg: "Step 005 Post-Software"
